# BWGI Python Challenge

Solução dos três desafios usando apenas a biblioteca padrão do Python.

Compatível com Python 3.9.

## Rodando

```bash
python -m unittest discover -s tests -v
```

## Arquivos

- `reconcile_accounts.py`
- `last_lines.py`
- `computed_property.py`
